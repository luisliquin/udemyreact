const activo = true;

// let mensaje ="";
// if (activo){
//     mensaje = "activo";
// }else{
//     mensaje = "inactivo";
// }
// const mensaje = (activo) ? 'activco' : 'inactivo';
// const mensaje = (activo) ? 'activco' : null;

const mensaje = activo === true && "activo"
console.log(mensaje)